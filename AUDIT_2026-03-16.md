# zero-explore 代码审计记录（2026-03-16）

## 范围

- `backend/src/main.rs`：RPC 错误处理与 redline（禁止 silent fallback）
- 文档补齐：`backend/README.md`、`frontend/README.md`、仓库根 `README.md`

## 关键发现与处置

### 1) RPC 错误被吞 / 静默降级（已修复）

问题：多个接口在 RPC 失败时使用 `unwrap_or(Value::Null)` / `.ok().unwrap_or(0)` 继续返回“看似正常”的数据，违反工作区 redlines：

- 默认 `fail-fast`
- 禁止 silent fallback
- 关键链路禁止吞错

修复：统一改为 **直接返回 `rpc_error`**（HTTP 502），并在必要字段缺失时返回错误：

- `network_stats`：`zero_getLatestBlock` 失败不再降级为 `null/0`
- `get_account_overview`：`zero_getAccount` / `zero_getUtxos` 失败不再降级
- `get_tx_detail`：`zero_getTransactionByHash` 失败不再降级为 `null`
- `search`：查询链上对象时不再吞错
- `get_overview`：`zero_listComputeTxResults` 失败不再默认为 `0`
- `latest_block_number`：缺失/非法 block number 不再返回 `0`

相关文件：`backend/src/main.rs`

### 2) 文档补齐（已完成）

- 新增：`backend/README.md`
- 新增：`frontend/README.md`
- 更新：仓库根 `README.md`（移除“旧节点自动回退”的描述，明确 fail-fast）

## 建议后续项（未改动）

- `parse_zero_block()` 仍对部分字段使用默认值（例如 `nonce/miner/tx_count`）；若需要更严格的 redline 语义，可考虑把解析从 `Option` 升级为 `Result`，在字段缺失时返回错误。
- `load_activity()` 在 state file JSON 解析失败时会回退为空状态；若认为该文件属于关键存储初始化链路，建议改为 fail-fast，或显式标注 `REDLINE_ALLOW` 并说明理由。
- 当前 `CorsLayer::permissive()` 允许所有来源；如需要部署到公网，建议结合实际场景收敛 CORS 策略。

## 验证

- `cargo test`（backend）通过

